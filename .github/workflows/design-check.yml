# .github/workflows/design-check.yml
#
# Runs the full design automation check suite on every push to main
# and on every pull request. Fails the build if any check exits non-zero.
# Uploads drift-report.html as a downloadable workflow artifact.

name: Design Check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  design-check:
    name: Design System Compliance
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        # version comes from the root package.json "packageManager" field (pnpm@10).

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run design checks
        run: pnpm -C apps/web design:check

      - name: Generate drift report
        if: always()
        run: pnpm -C apps/web design:report

      - name: Upload drift report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: design-drift-report
          path: apps/web/design/drift-report.html
          retention-days: 14
