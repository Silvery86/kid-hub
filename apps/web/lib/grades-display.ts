/**
 * Owned by @kid-hub/shared (Phase 4 — MOBILE_UI_IMP.md §7) so the grades screen
 * computes identically on both platforms. Re-exported here so existing web
 * imports of `@/lib/grades-display` keep working.
 */
export type { DisplayGradeRow } from '@kid-hub/shared'
export { gradesForSemester, semesterAverage, topSubjectForSemester } from '@kid-hub/shared'
