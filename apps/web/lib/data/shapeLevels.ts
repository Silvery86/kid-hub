/**
 * Shape Quest mini-game question generator.
 * Owned by @kid-hub/shared (Phase 3); re-exported so existing
 * `@/lib/data/shapeLevels` imports keep resolving.
 */
export { generateShapeQuestions } from '@kid-hub/shared'
