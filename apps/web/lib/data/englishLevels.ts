/**
 * English (Word Explorer) mini-game question generators.
 * Owned by @kid-hub/shared (Phase 3 — mobile_imp.md §10); re-exported so existing
 * `@/lib/data/englishLevels` imports keep resolving. Web and Mobile share one source.
 */
export {
  generateLetterMatchQuestions,
  generatePictureWordQuestions,
  generateAlphabetQuestions,
  generateWordSafariQuestions,
  generateSoundHuntQuestions,
} from '@kid-hub/shared'
