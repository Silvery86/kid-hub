/**
 * Math mini-game question generator.
 * Owned by @kid-hub/shared (Phase 3 — mobile_imp.md §10); re-exported so existing
 * `@/lib/data/mathLevels` imports keep resolving. Web and Mobile share one source.
 */
export { generateMathQuestions } from '@kid-hub/shared'
